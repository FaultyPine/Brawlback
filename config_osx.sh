# bin/sh

# Path to SD CARD Directory (this is the default paththat your dolphin instance uses)
export SDDIR="$HOME/Library/Application Support/Dolphin/Wii"

# filename of the SD card inside the SD Directory
export SDPATH="$SDDIR/sd.raw"

# Volume name of the SD CARD when mounted
export SDNAME="DOLPHINSD"